[package]
edition = "2018"
name = "pr0t0n_orch_db"
version = "0.1.0"

[dependencies]
diesel = {version = "1.4.4", features = ["postgres"]}
diesel_codegen = {version = "0.16.0", features = ["postgres"]}
dotenv = "0.15.0"
